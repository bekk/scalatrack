\frametitle {Actors - eksempler eksempler}\begin {content}
    \begin{lstlisting}
      case class Tick

      class Counter extends Actor {
        private var counter = 0

        def receive = {
          case Tick =>
            counter += 1
            println(counter)
        }
      }
    \end{lstlisting}
  \end{content}
